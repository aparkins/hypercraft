import {Voxel} from './entities/voxel.js';

class World {
    constructor() {
        this.voxels = [];
    }

    addVoxel(posX, posY, posZ, type) {
        this.voxels.push()
    }
};

export { World };
